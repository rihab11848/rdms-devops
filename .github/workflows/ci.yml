name: RDMS CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Display Success Message
        run: echo "CI Pipeline Running Successfully!"

      - name: Run Dummy Test
        run: echo "All Tests Passed!"
